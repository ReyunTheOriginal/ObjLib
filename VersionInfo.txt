Version: v4.6.0-Beta

Changes:
- Added Time with timescale, deltatime and RealTime
- Added FPS with SetTargetFPS(), FPS, and ExactFPS
- made it so instead of SpriteRenderers holding a texture for each renderer, the sprite holds the texture and the sprite renderer simply uses it
- some general optimizations